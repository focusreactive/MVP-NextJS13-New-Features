```js
export async function generateMetadata({ params }: { params: { id: string } }) {
  const [post] = await api.post(params.id);

  return {
    title: post.title,
  };
}

const PostPage = async ({ params }: { params: { id: string } }) => {
  const [post] = await api.post(params.id);
  const [image] = await api.images();
  const [user] = await api.user(post.userId);

  const jsonLd = {
    '@context': 'https://schema.org',
    '@type': 'BlogPosting',
    headline: post.title,
    image: image,
    publisher: user.name,
    datePublished: new Date().toISOString(),
    articleBody: post.body,
    author: {
      '@type': 'Person',
      name: user.name,
    },
  };

  return (
    <div>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
      />

      <article>
        <h2>{post.title}</h2>

        <p>{post.body}</p>

        <div className="grid">
          <img src={image} alt="dog" width={400} />
        </div>

        <Link href={`/parallel/users/${post.userId}`} className={'secondary'}>
          Author Page
        </Link>
      </article>
    </div>
  );
};
```

---

# Page Dynamic Metadata

```js
export async function generateMetadata({ params }: { params: { id: string } }) {
  const [post] = await api.post(params.id);

  return {
    title: post.title,
  };
}
```

---

# JSON LD

[Docs](https://nextjs.org/docs/app/building-your-application/optimizing/metadata#json-ld)

[This page in Google Reach Results](https://search.google.com/test/rich-results/result?id=RpXQvngfOGeNa0WigNfAEA)

Put the JSON LD script right in the page component:

```js
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
/>
```
