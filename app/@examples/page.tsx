const HomePage = () => {
  return (
    <div className={'rerender-indicator'}>
      <h1>
        During the presentation, the screen will be divided into two parts. On
        the left side the user interface will be displayed and on the right side
        there will be documents/description.
      </h1>
    </div>
  );
};

export default HomePage;
