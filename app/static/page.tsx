export const metadata = {
  title: 'Static',
};

const Page = () => {
  return (
    <div className={'rerender-indicator'}>
      <h1>Static + Cache examples</h1>
    </div>
  );
};

export default Page;
