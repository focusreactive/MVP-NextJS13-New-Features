export const metadata = {
  title: 'Parallel page',
};

const Page = () => {
  return (
    <div className={'rerender-indicator'}>
      <h1>Examples with parallel routes</h1>
    </div>
  );
};

export default Page;
